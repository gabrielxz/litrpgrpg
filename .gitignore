# Build artifacts
build/

# Editor / OS noise
*.swp
*.swo
.DS_Store
.vscode/
.idea/
__pycache__/
*.pyc

# Working images dropped in the repo root by the image tools
/*.png

# The annotated read-through copy
reading/

# The companion app's dependencies
node_modules/

# Local secrets (Supabase, Fly); never committed
.env
.env.*
!.env.example

# The web client build (served by the server; built in the Docker image)
app/packages/web/dist/
